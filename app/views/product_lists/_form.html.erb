<%= form_with(model: product_list) do |form| %>
  <% if product_list.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(product_list.errors.count, "error") %> prohibited this product_list from being saved:</h2>

      <ul>
        <% product_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name, autofocus: true, required: true %>
  </div>

  <% if product_list.persisted? %>
    <div>
      <%= form.label :favorited, style: "display: block" %>
      <%= form.check_box :favorited %>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
