<%= form_with(model: [product_list, product_item]) do |form| %>
  <% if product_item.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(product_item.errors.count, "error") %> prohibited this product_item from being saved:</h2>

      <ul>
        <% product_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name, autofocus: true, required: false %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
